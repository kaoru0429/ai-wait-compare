# 這次要等多久？

給非工程使用者的單頁工具：**選模型 → 貼 prompt → 看預估等待時間**。

- 示範模型為虛構數字，頁面有標示。
- 預估：`總時間 ≈ 開始前等待 + 回答長度換算後的輸出時間`（可依 prompt 長度略加等待）。
- 資料存在瀏覽器 `localStorage`（`ai-wait-compare-v2-simple`）。

預覽：https://kaoru0429.github.io/ai-wait-compare/
